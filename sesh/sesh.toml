#:schema https://github.com/joshmedeski/sesh/raw/main/sesh.schema.json

# Use a slightly longer derived name to reduce collisions between repos that
# share a basename under different parents.
dir_length = 2

# Speeds up repeated picker opens without changing session semantics.
cache = false
